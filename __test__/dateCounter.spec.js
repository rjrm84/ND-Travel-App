import { dateCounter } from '../src/client/js/dateCounter';
import { today } from '../src/client/js/today'

describe('testing the date counter functionality', () => {
    test('tests dateCounter() function', () => {
        //    input
        // expected output
        // expect()
        expect(dateCounter).toBeDefined();
    })
})
